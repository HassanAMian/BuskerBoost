<%= form_for(@status) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Tell us about it..." %>
  </div>
  <%= f.submit "Share", class: "btn btn-large btn-success" %>
<% end %>